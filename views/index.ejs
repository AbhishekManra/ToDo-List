<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/styles.css">
    <title>To - Do list</title>
</head>
<body>

    <div class="box" id="heading">
        <h1><%=Header%></h1>
    </div>

    <div class="box">
        <%array.forEach(data => {%>
        <div class="item">
            <form action="/delete" method="post">
                <input type="checkbox" name="chk" onChange="this.form.submit()" value=<%=data._id%>>
                <input type="hidden" name="h_box" value=<%=Header%>>
            </form>
            <p><%=data.name%></p>
        </div>
            <%});%>
            <form class="item" action="/" method="post" class="item">
                <input type="text" autocomplete="off" name="inp">
                <button name="btn" type="submit" value=<%=Header%>>+</button>
            </form>
    </div>
</body>
</html>